## 📌 Quando usar Tool Calling (abordagem recomendada)

Tool Calling é a abordagem moderna e recomendada pelo LangChain para integrar LLMs
com ferramentas externas de forma segura, robusta e determinística.

### Nessa abordagem:
- O modelo não precisa escrever ações em texto livre
- As chamadas de tools são feitas de forma estruturada (JSON interno)
- O LangChain controla quando e como a ferramenta é executada
- O LLM não consegue “inventar” observações ou pular a execução da tool

### Esse método é ideal para:
- Código de produção
- Exemplos introdutórios que precisam ser confiáveis
- Ferramentas determinísticas (cálculos, buscas, APIs, bancos de dados)
- Situações onde a resposta final DEVE refletir exatamente o retorno da tool

## 🔍 Sobre este exemplo específico:
Neste cenário, Tool Calling é a escolha correta porque:
- As decisões são simples (qual tool usar)
- Não há ganho em expor o raciocínio interno do modelo
- Queremos garantir que o resultado da tool seja sempre respeitado
- Evitamos erros comuns do padrão ReAct textual

👉 Sempre que o foco for confiabilidade, previsibilidade e execução correta
das ferramentas, Tool Calling deve ser preferido em relação ao ReAct clássico.